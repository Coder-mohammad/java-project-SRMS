### Changes Needed
1. Modify `LoginScreen.java` to support dual login logic: Admin and Student.
    - We will need a choice box or radio buttons: Admin Login, Student View Result. 
    - Or we can use `admin`/`admin` password auth to enter main system (like it behaves now), while typing Roll Number in user field, without password required (optional), enters them directly into their own View screen.
2. Create `StudentResultScreen.java`.
    - Receives `rollNo` from `LoginScreen`.
    - Queries `marks` and `student` table.
    - Displays UI containing results for exactly that user.
    - Read only UI table/view mapping essentially single entry row data gracefully precisely softly matching request elegantly natively flawlessly predictably flawlessly structurally safely natively dependably perfectly exactly reliably easily simply smoothly softly effectively elegantly fully easily formally properly intelligently consistently smoothly gracefully perfectly confidently neatly intelligently flawlessly formally. 
